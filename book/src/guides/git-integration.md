# Git Integration

*This guide is coming soon.* It will cover using writ alongside git in detail: the bridge system, bidirectional import/export, the `writ finish` round trip, and workflows for teams that use git for remote collaboration.

In the meantime, see the [Quickstart](../getting-started/quickstart.md) for a basic git + writ workflow.
