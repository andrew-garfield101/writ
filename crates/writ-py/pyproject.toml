[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "writ"
version = "0.1.0"
description = "Python bindings for writ — AI-native version control"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
